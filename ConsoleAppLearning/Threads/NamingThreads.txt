using System;
using System.Threading;

class NameExample
{
    static void Task()
    {
        Console.WriteLine(Thread.CurrentThread.Name);
    }

    static void Main()
    {
        Thread t1 = new Thread(Task);
        t1.Name = "Worker 1";

        t1.Start();
    }
}